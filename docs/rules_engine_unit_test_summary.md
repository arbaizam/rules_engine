# Rules Engine Unit Test Summary

Source: current pytest suite under `tests/`.

This summary was generated from the current unit-test source. Test IDs are stable within this generated document and are ordered by test file and function name. Descriptions include the source pytest function for traceability.

The current suite contains 104 unit tests across 15 test files.

| TestID | Area | Priority | Description | ExpectedResult |
| --- | --- | --- | --- | --- |
| UT-001 | YAML compilation | High | Compiles a minimal row-level YAML rule with owner metadata. Source: `tests/test_compiler_yaml.py::test_valid_simple_row_rule_compiles_and_validates`. Details: Basic YAML authoring must produce canonical condition metadata. | Regression is detected when: Required fields, owner fields, default tolerance, or eq parsing regress. |
| UT-002 | YAML compilation | High | Compiles a group-scoped aggregate with an aggregate filter. Source: `tests/test_compiler_yaml.py::test_valid_group_aggregate_rule_compiles`. Details: Group aggregates are first-class operands and must preserve by/filter shape. | Regression is detected when: Aggregate scope, by fields, or filtered aggregate parsing changes. |
| UT-003 | YAML compilation | High | Compiles a dataset-scoped aggregate. Source: `tests/test_compiler_yaml.py::test_valid_dataset_aggregate_rule_compiles`. Details: Dataset aggregates must be expressible without group-by fields. | Regression is detected when: Dataset scope is misparsed or implicit group fields leak in. |
| UT-004 | YAML compilation | High | Compiles all canonical string operators. Source: `tests/test_compiler_yaml.py::test_canonical_string_operators_compile`. Details: The engine explicitly supports these operators without aliases. | Regression is detected when: Operator enum parsing drops or renames a canonical string operator. |
| UT-005 | YAML compilation | High | Rejects the non-canonical operand key value. Source: `tests/test_compiler_yaml.py::test_value_operand_alias_is_rejected`. Details: Authoring must use literal so persisted metadata remains deterministic. | Regression is detected when: Alias support is accidentally reintroduced in the YAML compiler. |
| UT-006 | YAML compilation | High | Rejects the non-canonical rule key assignments. Source: `tests/test_compiler_yaml.py::test_assignments_rule_alias_is_rejected`. Details: Authoring must use assign to avoid multiple spellings for the same concept. | Regression is detected when: The compiler accepts legacy or convenience aliases. |
| UT-007 | YAML compilation | High | Rejects the non-canonical aggregate key field_name. Source: `tests/test_compiler_yaml.py::test_aggregate_field_name_alias_is_rejected`. Details: Aggregate authoring must use field as the single accepted vocabulary. | Regression is detected when: Aggregate aliases are accepted or canonical key enforcement weakens. |
| UT-008 | Cross-runtime parity | Critical | Compares Python and Spark results for a simple row comparison. Source: `tests/test_cross_runtime_parity.py::test_cross_runtime_parity_row_comparison`. Details: Spark row UDF behavior should preserve reference runtime semantics. | Regression is detected when: Basic equality matching diverges between runtimes. |
| UT-009 | Cross-runtime parity | Critical | Compares Python and Spark results for SQL LIKE wildcard matching. Source: `tests/test_cross_runtime_parity.py::test_cross_runtime_parity_string_operator`. Details: LIKE was a prior semantic divergence risk and needs parity coverage. | Regression is detected when: Python regex LIKE and Spark Column.like disagree. |
| UT-010 | Cross-runtime parity | Critical | Compares Python and Spark results for dataset aggregate evaluation. Source: `tests/test_cross_runtime_parity.py::test_cross_runtime_parity_dataset_aggregate`. Details: Aggregate precompute must preserve reference runtime results. | Regression is detected when: Spark dataset aggregate values differ from Python aggregate values. |
| UT-011 | Cross-runtime parity | Critical | Compares Python and Spark results for group aggregate evaluation. Source: `tests/test_cross_runtime_parity.py::test_cross_runtime_parity_group_aggregate`. Details: Explicit group scope must behave identically across runtimes. | Regression is detected when: Spark grouping/joining diverges from Python group-key resolution. |
| UT-012 | Cross-runtime parity | Critical | Compares Python and Spark results for filtered aggregate evaluation. Source: `tests/test_cross_runtime_parity.py::test_cross_runtime_parity_filtered_aggregate`. Details: Filtered aggregate semantics must not depend on runtime implementation. | Regression is detected when: Spark filter precompute and Python filter evaluation diverge. |
| UT-013 | Cross-runtime parity | Critical | Compares Python and Spark results for FIRST desc ordering with nulls. Source: `tests/test_cross_runtime_parity.py::test_cross_runtime_parity_first_with_desc_null_ordering`. Details: Descending null ordering was a prior cross-runtime divergence risk. | Regression is detected when: Either runtime changes null placement for order-sensitive aggregates. |
| UT-014 | YAML export | High | Exports a compiled ruleset to YAML and recompiles it. Source: `tests/test_exporter_yaml.py::test_yaml_export_round_trips_compiled_ruleset`. Details: Governance workflows need stable YAML round-trip authoring support. | Regression is detected when: Exported YAML loses metadata, nested groups, aggregates, or assignments. |
| UT-015 | YAML export | High | Verifies YAML export emits canonical authoring keys only. Source: `tests/test_exporter_yaml.py::test_yaml_export_uses_canonical_keys`. Details: Exported YAML must not reintroduce legacy aliases or internal dataclass names. | Regression is detected when: Export uses value/assignments aliases or omits canonical group IDs. |
| UT-016 | Pipeline versioning | Medium | Compares numeric dot-notation versions using integer segments. Source: `tests/test_pipeline_versioning.py::test_compare_versions_orders_numeric_dot_versions`. Details: Production auto-retire must not publish older YAML over a newer version. | Regression is detected when: Version ordering falls back to lexicographic string comparison. |
| UT-017 | Pipeline versioning | Medium | Rejects non-numeric versions for automatic retirement. Source: `tests/test_pipeline_versioning.py::test_parse_numeric_version_rejects_tags_and_dates`. Details: The production cutover guard intentionally avoids tag/date semantics. | Regression is detected when: Ambiguous versions can bypass numeric version ordering. |
| UT-018 | Publishing | High | Rejects publish when the incoming ruleset status is not published. Source: `tests/test_publish.py::test_publish_requires_published_status`. Details: The lifecycle now supports only published and retired persisted states. | Regression is detected when: Retired models can be written through the publish path. |
| UT-019 | Publishing | High | Passes published_by during direct publication. Source: `tests/test_publish.py::test_publish_passes_provenance_to_repository`. Details: Publication writes the publication actor in one step. | Regression is detected when: The actor value is lost across the service/repository boundary. |
| UT-020 | Publishing | High | Passes effective date overrides during direct publication. Source: `tests/test_publish.py::test_publish_passes_effective_dates_to_repository`. Details: Effective dating is persisted lifecycle metadata controlled at publish time. | Regression is detected when: Effective dates are dropped before repository persistence. |
| UT-021 | Publishing | High | Allows publish callers to omit actor metadata. Source: `tests/test_publish.py::test_publish_allows_omitted_provenance`. Details: Repository defaults omitted actor values for locked-down production jobs. | Regression is detected when: Optional provenance becomes mandatory at service level. |
| UT-022 | Recon translation | Medium | Translates a flat all-AND reconciliation rule into canonical YAML. Source: `tests/test_recon_translation.py::test_all_and_source_rule_translates_correctly`. Details: All-AND chains are the baseline source pattern for recon migration. | Regression is detected when: JoinType handling, assignment output, or audit grouping regresses. |
| UT-023 | Recon translation | Medium | Verifies translator-level stop_on_match can be disabled. Source: `tests/test_recon_translation.py::test_translator_can_disable_stop_on_match`. Details: Some callers need metadata to return all matching rules instead of first match. | Regression is detected when: The translator ignores the configured multi-match policy. |
| UT-024 | Recon translation | Medium | Translates a mixed AND/OR chain using confirmed left-to-right semantics. Source: `tests/test_recon_translation.py::test_mixed_join_chain_translates_left_to_right`. Details: Manual PySpark JoinType logic chains operators from each row to the next row. | Regression is detected when: The translator reintroduces precedence assumptions or wrong grouping. |
| UT-025 | Recon translation | Medium | Translates grouped source criteria using GroupSequence and GroupJoinOperator. Source: `tests/test_recon_translation.py::test_group_sequence_translates_with_group_join_operator`. Details: Group metadata resolves source grouping that cannot be inferred from JoinType alone. | Regression is detected when: Group ordering, group joins, or audit pattern detection regresses. |
| UT-026 | Recon translation | Medium | Maps every supported source ValueOperator to its canonical operator. Source: `tests/test_recon_translation.py::test_operator_mapping_for_all_supported_source_operators`. Details: The translation utility must preserve source semantics without aliases. | Regression is detected when: A supported operator maps incorrectly or stops translating. |
| UT-027 | Recon translation | Medium | Deduplicates translated rule IDs when source rule names slug-collide. Source: `tests/test_recon_translation.py::test_slug_collision_emits_unique_rule_ids_and_audit_warning`. Details: Rule IDs drive audit output and must remain unique after normalization. | Regression is detected when: Collision handling creates duplicate IDs or omits audit warnings. |
| UT-028 | Recon translation | Medium | Rejects an unsupported source ValueOperator. Source: `tests/test_recon_translation.py::test_invalid_source_operator_fails_translation`. Details: The migration utility must fail explicitly instead of guessing semantics. | Regression is detected when: Unsupported operators are silently converted or omitted. |
| UT-029 | Recon translation | Medium | Rejects a non-final source row with a null JoinType. Source: `tests/test_recon_translation.py::test_null_join_before_final_criterion_fails_translation`. Details: Null JoinType terminates the source chain and later criteria are ambiguous. | Regression is detected when: The translator accepts disconnected criteria without audit failure. |
| UT-030 | Recon translation | Medium | Rejects a final source group that still declares a GroupJoinOperator. Source: `tests/test_recon_translation.py::test_last_group_join_operator_fails_translation`. Details: A final group has no next group, so the join operator cannot be applied. | Regression is detected when: Dangling group joins are ignored instead of reported. |
| UT-031 | Recon translation | Medium | Emits YAML text using the rules engine authoring vocabulary. Source: `tests/test_recon_translation.py::test_output_yaml_aligns_to_engine_authoring_format`. Details: Translated specs must compile as ordinary canonical authoring YAML. | Regression is detected when: Writer output drifts from compiler-supported YAML shape. |
| UT-032 | Recon translation | Medium | Writes translation audit records with required governance fields. Source: `tests/test_recon_translation.py::test_audit_artifact_contains_expected_fields`. Details: One-time migrations need a durable explanation of warnings and failures. | Regression is detected when: Audit serialization drops source identity, counts, warnings, or failures. |
| UT-033 | Repository persistence | Critical | Uses ruleset_name/version as the duplicate publish boundary. Source: `tests/test_repository.py::test_save_published_checks_duplicate_ruleset_name_and_version`. Details: Testing workflows may publish multiple ruleset IDs under the same user-facing ruleset/version identity. | Regression is detected when: The repository checks ruleset_id/version instead of ruleset_name/version. |
| UT-034 | Repository persistence | Critical | Allows two published versions with the same ruleset_name when their versions differ. Source: `tests/test_repository.py::test_save_published_allows_distinct_versions_for_same_ruleset_name`. Details: Candidate rulesets need to be published side by side for testing. | Regression is detected when: The repository still enforces a single published sibling per ruleset_name. |
| UT-035 | Repository persistence | Critical | Persists explicit effective dates on the ruleset version row. Source: `tests/test_repository.py::test_save_published_persists_effective_dates`. Details: Effective dating is queryable lifecycle metadata for published versions. | Regression is detected when: Publish drops effective-date overrides before serialization. |
| UT-036 | Repository persistence | Critical | Retirement updates lifecycle status and effective_end_date. Source: `tests/test_repository.py::test_retire_closes_effective_window`. Details: Retired versions should retain the date their effective window closed. | Regression is detected when: Retirement leaves effective_end_date open-ended. |
| UT-037 | Repository persistence | Critical | Allows retirement callers to close the effective window explicitly. Source: `tests/test_repository.py::test_retire_allows_explicit_effective_end_date`. Details: Backdated or end-of-business retirements may differ from the audit timestamp. | Regression is detected when: Explicit retirement effective_end_date is ignored. |
| UT-038 | Repository persistence | Critical | Rejects a second retirement for an already retired version. Source: `tests/test_repository.py::test_retire_rejects_already_retired_version`. Details: Retirement audit fields should preserve the original retirement event. | Regression is detected when: A second retirement silently overwrites retired_at or effective_end_date. |
| UT-039 | Repository persistence | Critical | Emits an insert-only merge when update_existing is disabled. Source: `tests/test_repository.py::test_save_function_registry_rows_skips_existing_rows_when_update_disabled`. Details: Deployment setup should load standard functions only when missing. | Regression is detected when: Existing function registry rows are overwritten during setup. |
| UT-040 | Repository persistence | Critical | Keeps custom function registry saves as upserts by default. Source: `tests/test_repository.py::test_save_function_registry_rows_upserts_by_default`. Details: Existing custom registry maintenance workflows may rely on replacement semantics. | Regression is detected when: The default save path stops updating matched function rows. |
| UT-041 | Repository schema | Critical | Verifies the ruleset version schema exposes payload, count, provenance, and hash columns. Source: `tests/test_repository_schema.py::test_ruleset_version_schema_contains_payload_provenance_and_hash_fields`. Details: These columns are the queryable contract for persisted ruleset metadata. | Regression is detected when: Schema refactors drop or rename required top-level fields. |
| UT-042 | Repository schema | Critical | Builds the standard two-table registry footprint from one schema name. Source: `tests/test_repository_schema.py::test_table_names_can_be_built_from_schema`. Details: Production pipeline configuration should not repeat fixed table names. | Regression is detected when: The schema helper stops producing the agreed registry table names. |
| UT-043 | Repository schema | Critical | Creates metadata tables through SQL DDL with NOT NULL columns. Source: `tests/test_repository_schema.py::test_create_base_tables_uses_explicit_delta_ddl_with_not_null_columns`. Details: Empty Spark DataFrame writes may drop nullable=False metadata in the catalog. | Regression is detected when: Table bootstrap returns to DataFrame-based creation or omits required nullability. |
| UT-044 | Repository schema | Critical | Uses CREATE TABLE IF NOT EXISTS for idempotent bootstrap. Source: `tests/test_repository_schema.py::test_create_base_tables_uses_if_not_exists_for_ignore_mode`. Details: Deployment setup should be safely rerunnable when requested. | Regression is detected when: Ignore mode emits destructive or non-idempotent DDL. |
| UT-045 | Python runtime | Critical | Evaluates a basic row-level rule and assignment in Python runtime. Source: `tests/test_runtime.py::test_runtime_evaluates_simple_row_rule_and_assignment`. Details: This is the reference behavior used by tests and Spark row UDFs. | Regression is detected when: Matching, assignment output, or rule trace generation regresses. |
| UT-046 | Python runtime | Critical | Emits resolved operand values and columns in rule_results. Source: `tests/test_runtime.py::test_runtime_rule_results_include_condition_trace_values`. Details: Runtime metadata must be traceable back to each evaluated condition. | Regression is detected when: rule_results falls back to compact rule-level pass/fail output. |
| UT-047 | Python runtime | Critical | Emits aggregate operand columns, group key, and resolved aggregate value. Source: `tests/test_runtime.py::test_runtime_rule_results_include_aggregate_trace_values`. Details: Aggregate-backed conditions must explain the value compared for a row. | Regression is detected when: aggregate traces omit source columns or evaluated values. |
| UT-048 | Python runtime | Critical | Emits custom-function argument traces and resolved function values. Source: `tests/test_runtime.py::test_runtime_rule_results_include_custom_function_arg_trace_values`. Details: Custom-function conditions need traceability for nested operand inputs. | Regression is detected when: function traces hide the row values passed into the callable. |
| UT-049 | Python runtime | Critical | Serializes the enriched rule-result payload through the Spark row UDF. Source: `tests/test_runtime.py::test_spark_row_evaluator_serializes_enriched_rule_results`. Details: Spark's JSON column must expose the same trace metadata as Python. | Regression is detected when: Spark keeps emitting only rule-level pass/fail metadata. |
| UT-050 | Python runtime | Critical | Evaluates contains/not_contains/starts_with/ends_with in runtime. Source: `tests/test_runtime.py::test_runtime_supports_canonical_string_operators`. Details: Canonical string operators must behave consistently after compilation. | Regression is detected when: Runtime string operator dispatch or matching semantics change. |
| UT-051 | Python runtime | Critical | Evaluates SQL LIKE percent wildcard behavior in Python runtime. Source: `tests/test_runtime.py::test_runtime_like_uses_sql_wildcard_semantics`. Details: Python and Spark runtimes must agree on LIKE semantics. | Regression is detected when: LIKE falls back to equality or mishandles SQL wildcards. |
| UT-052 | Python runtime | Critical | Evaluates null_result_mode=default on a null comparison result. Source: `tests/test_runtime.py::test_runtime_null_result_default_controls_condition_result`. Details: Null handling is explicit metadata and controls final condition truth. | Regression is detected when: Null defaults are ignored or treated as ordinary nulls. |
| UT-053 | Python runtime | Critical | Resolves and executes a registered custom function operand. Source: `tests/test_runtime.py::test_runtime_executes_custom_function_operand`. Details: Custom logic is allowed only through the registry contract. | Regression is detected when: Runtime cannot look up or call registered custom functions. |
| UT-054 | Python runtime | Critical | Evaluates a dataset-scoped aggregate against all rows. Source: `tests/test_runtime.py::test_runtime_evaluates_dataset_aggregate`. Details: Dataset aggregates must apply to the incoming row set exactly as supplied. | Regression is detected when: Dataset aggregate caching or row application semantics regress. |
| UT-055 | Python runtime | Critical | Evaluates a group-scoped aggregate using the current row's group key. Source: `tests/test_runtime.py::test_runtime_evaluates_group_aggregate_against_current_row_group`. Details: Group aggregates must return different values for different groups. | Regression is detected when: Group-key resolution or aggregate cache partitioning regresses. |
| UT-056 | Python runtime | Critical | Evaluates a dataset aggregate with a row-level filter. Source: `tests/test_runtime.py::test_runtime_evaluates_filtered_aggregate`. Details: Filtered aggregates should filter only aggregate inputs, not output rows. | Regression is detected when: Filter predicates are ignored or applied to the outer row set. |
| UT-057 | Python runtime | Critical | Evaluates FIRST aggregate with explicit ascending order_by. Source: `tests/test_runtime.py::test_runtime_evaluates_order_sensitive_first_aggregate`. Details: Order-sensitive aggregates require deterministic ordering. | Regression is detected when: FIRST ignores order_by or uses input order accidentally. |
| UT-058 | Python runtime | Critical | Evaluates FIRST with descending order and null order values. Source: `tests/test_runtime.py::test_runtime_desc_ordering_keeps_nulls_last_for_first_aggregate`. Details: Python and Spark runtimes intentionally keep nulls last for ordering. | Regression is detected when: Descending sort places nulls first or diverges from Spark. |
| UT-059 | Python runtime | Critical | Calls Spark aggregate support guard for median. Source: `tests/test_runtime.py::test_spark_runtime_fails_fast_for_exact_median_gap`. Details: Spark exact median/quantile semantics are intentionally unsupported in v1. | Regression is detected when: Approximate Spark percentile paths become silently reachable. |
| UT-060 | Serialization | Critical | Serializes a ruleset and inspects explicit canonical payload fields. Source: `tests/test_serializer.py::test_serializer_persists_canonical_payload_with_explicit_fields`. Details: Persisted payload_json is the audit source for runtime reconstruction. | Regression is detected when: Scope, tolerance, or null fields are omitted or renamed. |
| UT-061 | Serialization | Critical | Serializes owner, lifecycle, hash, and payload summary metadata. Source: `tests/test_serializer.py::test_serializer_stamps_provenance_hash_and_summary_counts`. Details: The ruleset_versions row must expose queryable audit metadata. | Regression is detected when: provenance, summary counts, or content_hash are not populated. |
| UT-062 | Serialization | Critical | Compares content_hash to SHA-256 of persisted payload_json bytes. Source: `tests/test_serializer.py::test_content_hash_equals_sha256_of_payload_json`. Details: Auditors must be able to independently recompute the integrity hash. | Regression is detected when: The hash is computed from a different or noncanonical payload. |
| UT-063 | Serialization | Critical | Serializes the same ruleset twice and compares payload/hash output. Source: `tests/test_serializer.py::test_content_hash_and_payload_json_are_deterministic`. Details: Promotion and audit comparisons require stable serialization. | Regression is detected when: Dict ordering or serialization paths produce nondeterministic bytes. |
| UT-064 | Serialization | Critical | Deserializes a persisted ruleset version back to canonical dataclasses. Source: `tests/test_serializer.py::test_deserializer_reconstructs_canonical_models`. Details: Runtime loading depends on payload_json round-tripping exactly. | Regression is detected when: Persistence loses fields or reconstructs a different ruleset. |
| UT-065 | Serialization | Critical | Confirms payload_json does not duplicate lifecycle status. Source: `tests/test_serializer.py::test_persisted_payload_excludes_lifecycle_status`. Details: The table status column is authoritative for publish/retire state. | Regression is detected when: Payload content and row lifecycle status can diverge. |
| UT-066 | Serialization | Critical | Persists effective dates as row metadata while keeping payload content canonical. Source: `tests/test_serializer.py::test_serializer_accepts_explicit_effective_dates_outside_payload`. Details: Effective dating belongs to publication lifecycle metadata, not authored YAML. | Regression is detected when: Effective dates are ignored or written into payload_json. |
| UT-067 | Serialization | Critical | Defaults the effective window when no explicit dates are supplied. Source: `tests/test_serializer.py::test_serializer_defaults_effective_dates_from_publish_metadata`. Details: Published rows should always have a non-null effective date range. | Regression is detected when: Default effective dates are missing or not derived from published_at. |
| UT-068 | Service facade | High | Builds a service from schema and checks repository table names. Source: `tests/test_service.py::test_service_from_schema_uses_standard_table_names`. Details: The facade should hide repetitive repository wiring without changing table names. | Regression is detected when: from_schema stops using RulesEngineTableNames.from_schema. |
| UT-069 | Service facade | High | Builds a service from schema while overriding metadata table names. Source: `tests/test_service.py::test_service_from_schema_accepts_custom_table_names`. Details: Deployments may need project-specific table names without manual repository wiring. | Regression is detected when: from_schema ignores custom table-name overrides. |
| UT-070 | Service facade | High | Publishes YAML through the facade and loads it back. Source: `tests/test_service.py::test_service_publish_yaml_text_and_loads_published_ruleset`. Details: Notebook users should have one public entry point for common publish/load work. | Regression is detected when: service wiring loses publish provenance or repository calls. |
| UT-071 | Service facade | High | Exercises table creation, standard function registry save, and retire facade calls. Source: `tests/test_service.py::test_service_create_tables_save_standard_functions_and_retire`. Details: These are common notebook/pipeline operations that should stay easy to call. | Regression is detected when: facade methods stop delegating to the repository. |
| UT-072 | Service facade | High | Saves caller-supplied function registry rows through the facade. Source: `tests/test_service.py::test_service_saves_supplied_function_registry_rows`. Details: Custom function notebooks should not need to reach through service.repository. | Regression is detected when: service stops exposing custom registry persistence. |
| UT-073 | Service facade | High | Allows standard function metadata to update existing registry rows by explicit request. Source: `tests/test_service.py::test_service_can_upsert_standard_function_registry_when_requested`. Details: Deployment setup defaults to insert-only, but controlled upgrades may need upsert behavior. | Regression is detected when: The update_existing option is not passed through. |
| UT-074 | Service facade | High | Rejects evaluate calls without a ruleset or ruleset name. Source: `tests/test_service.py::test_service_evaluate_dataframe_requires_ruleset_or_name`. Details: Silent evaluation against no ruleset would hide caller errors. | Regression is detected when: evaluate_dataframe accepts ambiguous input. |
| UT-075 | Service facade | High | Formats compiled rule metadata into readable table-shaped rows. Source: `tests/test_service.py::test_service_describe_rules_formats_supplied_ruleset`. Details: Notebook users need a compact audit view of rule logic and match payloads. | Regression is detected when: Service-level rule descriptions expose raw metadata or omit payload details. |
| UT-076 | Service facade | High | Loads a published ruleset and renders nested condition groups. Source: `tests/test_service.py::test_service_describe_rules_loads_published_ruleset_and_formats_nested_logic`. Details: The service helper should work for persisted rules without losing boolean structure. | Regression is detected when: Nested groups are flattened ambiguously or repository loading is bypassed. |
| UT-077 | Service facade | High | Rejects describe calls without a ruleset or ruleset name. Source: `tests/test_service.py::test_service_describe_rules_requires_ruleset_or_name`. Details: The helper should fail clearly like evaluate_dataframe. | Regression is detected when: describe_rules accepts ambiguous input. |
| UT-078 | Service facade | High | Publishes an already compiled ruleset through the facade. Source: `tests/test_service.py::test_service_publish_accepts_compiled_ruleset`. Details: Code-authored and precompiled YAML workflows should share the same service. | Regression is detected when: service.publish only supports YAML text/path entry points. |
| UT-079 | Service facade | High | Passes effective date overrides through the facade. Source: `tests/test_service.py::test_service_publish_and_retire_pass_effective_dates`. Details: Notebook callers should not need to reach into lower-level services for lifecycle dating. | Regression is detected when: Service methods drop effective-date arguments. |
| UT-080 | Spark runtime | Critical | Evaluates a row-level rule through Spark DataFrame runtime. Source: `tests/test_spark_runtime.py::test_spark_runtime_evaluates_row_rule`. Details: Spark output columns must reflect the same matching/assignment contract. | Regression is detected when: UDF result struct, output columns, or assignment JSON regress. |
| UT-081 | Spark runtime | Critical | Evaluates a dataset aggregate through Spark precompute plus UDF. Source: `tests/test_spark_runtime.py::test_spark_runtime_evaluates_dataset_aggregate`. Details: Spark must join dataset aggregate results back to every input row. | Regression is detected when: Dataset aggregate precompute or cross join behavior changes. |
| UT-082 | Spark runtime | Critical | Evaluates a group aggregate through Spark precompute plus UDF. Source: `tests/test_spark_runtime.py::test_spark_runtime_evaluates_group_aggregate`. Details: Spark must compute and join aggregate values by explicit group keys. | Regression is detected when: Group aggregation, join keys, or per-row lookup regresses. |
| UT-083 | Spark runtime | Critical | Evaluates a Spark dataset aggregate with an aggregate filter. Source: `tests/test_spark_runtime.py::test_spark_runtime_evaluates_filtered_aggregate`. Details: Spark precompute must filter aggregate inputs without filtering output rows. | Regression is detected when: Filter expression generation or aggregate input selection regresses. |
| UT-084 | Spark compatibility validation | High | Rejects exact median and quantile aggregates for Spark publication. Source: `tests/test_spark_validator.py::test_spark_validator_rejects_median_and_quantile`. Details: Spark v1 runtime does not provide exact percentile parity with Python. | Regression is detected when: Approximate percentile paths become publishable for Spark rulesets. |
| UT-085 | Spark compatibility validation | High | Rejects aggregate null modes that Spark precomputation cannot honor. Source: `tests/test_spark_validator.py::test_spark_validator_rejects_unsupported_aggregate_null_modes`. Details: Spark aggregate columns are computed before row UDF evaluation. | Regression is detected when: Runtime-only aggregate null errors can be published. |
| UT-086 | Spark compatibility validation | High | Rejects FIRST/LAST aggregates with propagate null input mode. Source: `tests/test_spark_validator.py::test_spark_validator_rejects_first_last_propagate`. Details: Spark order-sensitive aggregate precompute does not support propagate semantics. | Regression is detected when: Spark-incompatible FIRST/LAST null behavior passes validation. |
| UT-087 | Spark compatibility validation | High | Rejects aggregate filter predicates using Spark-unsupported null error modes. Source: `tests/test_spark_validator.py::test_spark_validator_rejects_unsupported_filter_null_modes`. Details: Filter predicates run in Spark expressions, not the Python row error path. | Regression is detected when: Unsupported filter null modes are silently coerced in Spark. |
| UT-088 | Spark compatibility validation | High | Allows condition-level null_result_mode=error for ordinary row UDF checks. Source: `tests/test_spark_validator.py::test_spark_validator_allows_condition_null_result_error_for_udf_row_path`. Details: Non-filter row conditions are evaluated inside the Spark Python row runtime. | Regression is detected when: Spark compatibility validation blocks supported row-level error modes. |
| UT-089 | Standard functions | Medium | Verifies substring uses a 1-based start index. Source: `tests/test_standard_functions.py::test_substring_uses_sql_style_start_position`. Details: Databricks/Spark authors commonly expect SQL substring semantics. | Regression is detected when: substring behaves like Python zero-based slicing. |
| UT-090 | Standard functions | Medium | Registers standard functions and evaluates substring against row fields. Source: `tests/test_standard_functions.py::test_standard_functions_can_be_registered_for_runtime_field_args`. Details: Common custom functions must be usable with dynamic row values in rules. | Regression is detected when: custom_function args remain literal-only metadata. |
| UT-091 | Standard functions | Medium | Creates persisted metadata rows for standard functions. Source: `tests/test_standard_functions.py::test_standard_function_rows_expose_registry_metadata`. Details: Production workflows can save function specs without hand-authoring them. | Regression is detected when: standard function specs cannot be written to the registry table. |
| UT-092 | Semantic validation | High | Validates that quantile aggregates require args.q. Source: `tests/test_validator.py::test_quantile_missing_q_fails_validation`. Details: Quantile semantics are undefined without an explicit requested quantile. | Regression is detected when: The validator allows quantile metadata with missing or implicit q. |
| UT-093 | Semantic validation | High | Validates that group-scoped aggregates require by fields. Source: `tests/test_validator.py::test_group_scope_without_by_fails_validation`. Details: Group scope is explicit and cannot infer grouping keys from context. | Regression is detected when: The validator permits implicit grouping for scope=group. |
| UT-094 | Semantic validation | High | Validates that owner and owner_department are required. Source: `tests/test_validator.py::test_missing_owner_metadata_fails_validation`. Details: Rulesets need business ownership metadata for governance and audit. | Regression is detected when: Rulesets can validate without required ownership fields. |
| UT-095 | Semantic validation | High | Validates that dataset-scoped aggregates reject by fields. Source: `tests/test_validator.py::test_dataset_scope_with_by_fails_validation`. Details: Dataset scope means the entire incoming row set, not grouped subsets. | Regression is detected when: Dataset aggregates can carry contradictory group-by metadata. |
| UT-096 | Semantic validation | High | Validates that first/last aggregates require order_by. Source: `tests/test_validator.py::test_order_sensitive_aggregate_without_order_by_fails_validation`. Details: Order-sensitive aggregates must not rely on implicit row ordering. | Regression is detected when: The validator permits nondeterministic first/last aggregates. |
| UT-097 | Semantic validation | High | Validates that aggregate filters cannot contain aggregate operands. Source: `tests/test_validator.py::test_nested_aggregate_in_filter_fails_validation`. Details: Nested aggregates are outside the v1 semantic contract. | Regression is detected when: Filter predicates can recursively depend on aggregate results. |
| UT-098 | Semantic validation | High | Validates custom function argument names against the registry contract. Source: `tests/test_validator.py::test_custom_function_args_mismatch_fails_validation`. Details: Runtime function calls must be deterministic and fully specified. | Regression is detected when: Missing, extra, or misspelled custom function args are accepted. |
| UT-099 | Semantic validation | High | Validates that null_result_mode=default requires null_default_value. Source: `tests/test_validator.py::test_null_result_mode_default_without_default_fails_validation`. Details: Default null behavior must be explicit in published metadata. | Regression is detected when: Conditions can silently default null results without a value. |
| UT-100 | Semantic validation | High | Validates a condition using a canonical string operator. Source: `tests/test_validator.py::test_valid_string_operators_validate`. Details: Supported string operators must pass semantic validation consistently. | Regression is detected when: String operator support is removed or misclassified. |
| UT-101 | Semantic validation | High | Validates that aggregate filters contain at least one predicate. Source: `tests/test_validator.py::test_empty_aggregate_filter_fails_validation`. Details: Empty filters diverge across runtimes and make aggregate semantics unclear. | Regression is detected when: filter all/any with an empty list can be published. |
| UT-102 | Semantic validation | High | Validates that between/not_between cannot use non-zero tolerance. Source: `tests/test_validator.py::test_between_with_nonzero_tolerance_fails_validation`. Details: v1 defines tolerance for scalar comparisons but not range expansion. | Regression is detected when: Metadata can persist a tolerance that runtime would ignore. |
| UT-103 | Semantic validation | High | Validates that condition_id values are unique within a ruleset. Source: `tests/test_validator.py::test_duplicate_condition_ids_fail_validation`. Details: Duplicate condition IDs make runtime traces and audit diagnostics ambiguous. | Regression is detected when: Code or YAML authoring can publish colliding condition IDs. |
| UT-104 | Semantic validation | High | Validates that condition_group_id values are unique within a ruleset. Source: `tests/test_validator.py::test_duplicate_condition_group_ids_fail_validation`. Details: Duplicate group IDs make audit diagnostics ambiguous for code-authored rulesets. | Regression is detected when: Nested condition groups can reuse the same identifier. |
