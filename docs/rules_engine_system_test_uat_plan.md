# Rules Engine System and UAT Test Summary

Source workbook: `outputs/rules_engine_system_test_uat_plan.xlsx`

This Markdown summary was generated by cross-referencing the dated workbook against the current notebook source files:

- `notebooks/rules_engine_system_tests.py`
- `notebooks/rules_engine_uat_tests.py`

The current code is treated as the source of truth for `TestID`, `Area`, `Priority`, and `ExpectedResult`. Workbook execution-step text is included in `Description` where it still maps to the current test. The current source contains 67 system tests and 24 UAT tests. Before ST-001, the system notebook also verifies that the imported package came from the installed wheel distribution, locates its repository fixtures without changing package precedence, and refuses schemas whose names do not visibly identify a disposable test/scratch/tmp target.

## System Tests

| TestID | Area | Priority | Description | ExpectedResult |
| --- | --- | --- | --- | --- |
| ST-001 | Deployment setup | Critical | Setup notebook creates the target schema before table creation. Details: Run the setup notebook against a clean test catalog/schema. Use CREATE SCHEMA IF NOT EXISTS before service.create_tables. | The schema exists and the notebook proceeds to table creation without manual pre-work. |
| ST-002 | Deployment setup | Critical | Rules engine metadata tables are created with standard names. Details: Instantiate RulesEngineService.from_schema with the target schema and call service.create_tables(mode='ignore'). | ruleset_versions and function_registry exist in the target schema. Rerunning the cell does not fail. |
| ST-003 | Deployment setup | High | Custom metadata table names are honored by the service. Details: Instantiate RulesEngineService.from_schema with ruleset_versions_table and function_registry_table overrides, then create tables. | The custom tables are created and service.table_names reports the custom names for later publish/load calls. |
| ST-004 | Deployment setup | Critical | Metadata table DDL preserves NOT NULL columns. Details: After table creation, DESCRIBE TABLE EXTENDED or catalog inspection is run for both metadata tables. | Required columns such as ruleset_id, ruleset_name, version, status, effective_start_date, effective_end_date, payload_json, content_hash, rule_count, function_name, implementation_reference, and active_flag are marked NOT NULL where expected. |
| ST-005 | Deployment setup | High | Overwrite mode is restricted to disposable environments. Details: Run setup in a disposable/dev schema using mode='overwrite'. Verify project guidance blocks or avoids overwrite for production-like schemas. | Overwrite recreates tables only in disposable schemas. Production setup uses mode='ignore' or 'error'. |
| ST-006 | Function registry | Critical | Standard function metadata is registered after setup. Details: Call service.save_standard_function_registry after table creation. | function_registry contains all package standard functions with expected implementation_reference, active_flag, allowed flags, return type, and version. |
| ST-007 | Function registry | High | Standard function registration is rerunnable and refreshes package-owned metadata. Details: Run service.save_standard_function_registry twice with its default update behavior. | Both runs succeed, row count remains stable, no duplicates appear, and persisted package versions match the installed standard-function catalog. |
| ST-008 | Function registry | Medium | Explicit preservation mode leaves existing registry rows unchanged. Details: Run service.save_standard_function_registry(update_existing=False) in a controlled test schema. | Existing rows are not overwritten and no duplicate function names are inserted. |
| ST-009 | Function registry | Critical | Runtime in-memory registry contains executable standard implementations. Details: Instantiate RulesEngineService.from_schema with default register_standard=True and inspect registry specs or run a ruleset using standard custom functions. | Standard functions are available for validation and runtime evaluation even though function_registry only stores metadata. |
| ST-010 | Function registry | High | Custom function metadata can be saved without breaking standard registry rows. Details: Save a test custom FunctionRegistryRow through service.save_function_registry_rows. | The custom function row is inserted or updated and existing standard rows remain present. |
| ST-011 | YAML compilation | Critical | Valid canonical ruleset YAML compiles through the service. Details: Call service.compile_yaml_path on a known good ruleset YAML artifact. | A Ruleset object is returned with expected ruleset_id, ruleset_name, version, owner, owner_department, rules, conditions, and assignments. |
| ST-012 | YAML compilation | High | YAML text compilation works for notebook-authored payloads. Details: Call service.compile_yaml_text on a canonical YAML string. | The text compiles into the same model shape as file-based compilation. |
| ST-013 | YAML compilation | High | Unsupported legacy aliases are rejected clearly. Details: Compile YAML using unsupported keys such as assignments or operand value. | Compilation fails with a clear message identifying the unsupported key and canonical replacement. |
| ST-014 | YAML compilation | Critical | Required top-level ruleset metadata is enforced. Details: Compile YAML missing ruleset_id, ruleset_name, version, or rules list. | Compilation fails before validation or persistence. Error identifies the missing or malformed field. |
| ST-015 | YAML compilation | High | Condition group logical operator shape is enforced. Details: Compile YAML with missing or multiple logical operator keys in a condition group. | Compilation fails and identifies that exactly one logical operator is required. |
| ST-016 | Semantic validation | Critical | Valid simple row-level rule passes validation. Details: Compile a simple ruleset using field/literal comparison and assignment, then run service.validator.validate. | Validation passes with no error-severity issues. |
| ST-017 | Semantic validation | Critical | Owner and owner_department are required before publish. Details: Validate a ruleset missing owner or owner_department. | Validation returns error-severity issues and publish is blocked. |
| ST-018 | Semantic validation | High | Duplicate condition_id values are rejected. Details: Validate a ruleset with duplicate condition IDs across rules or nested groups. | Validation returns a duplicate condition ID error. |
| ST-019 | Semantic validation | High | Duplicate condition_group_id values are rejected. Details: Validate a ruleset with duplicate group IDs. | Validation returns a duplicate condition group ID error. |
| ST-020 | YAML compilation | Critical | Aggregate operands are rejected. Details: Compile YAML containing an `aggregate` operand. | Compilation fails with guidance to precompute aggregate values upstream and reference them as fields. |
| ST-021 | YAML compilation | High | Precomputed aggregate facts compile as fields. Details: Compile a ruleset that references a column such as `account_amount_sum`. | The field operand compiles and evaluates like any other row-level field. |
| ST-022 | Semantic validation | High | Custom-function argument contracts are enforced. Details: Validate a rule with missing, extra, or misspelled function arguments. | Validation reports the registered contract mismatch. |
| ST-023 | Semantic validation | High | Operator arity and literal collection requirements are enforced. Details: Validate operators such as is_null, between, in, and not_in with invalid right operands. | Validation identifies missing, extra, or malformed operands. |
| ST-024 | Semantic validation | Medium | Null handling modes require default values when configured. Details: Validate null_result_mode=default without null_default_value. | Validation returns a default-value requirement error. |
| ST-025 | Spark compatibility | Critical | Spark validator accepts supported rulesets. Details: Run SparkRulesetCompatibilityValidator through service.validator on representative supported rulesets. | Validation passes and no Spark-specific unsupported-operation issues are present. |
| ST-026 | Spark compatibility | High | Spark validator follows the supported row-level contract. Details: Validate rulesets with fields, literals, and registered custom functions. | Validation passes for supported row-level rules and rejects base semantic errors. |
| ST-027 | Publishing | Critical | Publish YAML path compiles, normalizes, validates, and writes metadata. Details: Call service.publish_yaml_path on a valid YAML artifact. | One published ruleset_versions row is written with status published, content_hash, payload_json, counts, owner, published_by, and published_at. |
| ST-028 | Publishing | Critical | Direct publish of a compiled ruleset works. Details: Compile YAML, validate it, then call service.publish(ruleset, published_by=...). | The compiled ruleset is persisted and loadable by name/version. |
| ST-029 | Publishing | Critical | Publish rejects non-published lifecycle status. Details: Attempt to publish a compiled ruleset with status retired. | Publish fails before persistence with a clear status requirement message. |
| ST-030 | Publishing | Critical | Duplicate ruleset_name and version cannot be published twice. Details: Publish a ruleset, then attempt to publish another ruleset with the same ruleset_name and version. | Second publish fails and does not overwrite the original row. |
| ST-031 | Publishing | High | Multiple published versions for the same ruleset_name are allowed. Details: Publish version 1 and version 2 for the same ruleset_name. | Both rows have status published. Neither publish requires retiring the other version. |
| ST-032 | Publishing | High | Loading by name without version is rejected when ambiguous. Details: With two published versions for the same ruleset_name, call service.load_published(ruleset_name) without version. | Repository raises an error asking the caller to specify version. |
| ST-033 | Publishing | Critical | Loading by name and version requires one immutable metadata row. Details: Load and evaluate a uniquely published version, append a duplicate of that exact metadata row, then load the same explicit version again. | The unique row reconstructs and evaluates correctly; the duplicate immutable version fails loudly instead of selecting an arbitrary row. |
| ST-034 | Lifecycle | Critical | Retirement makes a version unavailable to load_published. Details: Publish a ruleset, call service.retire(ruleset_id, version), then attempt load_published by name/version. | The row status is retired and load_published no longer returns it. |
| ST-035 | Lifecycle | High | Retirement stamps retired_by and retired_at. Details: Retire a published version with retired_by populated. | retired_by and retired_at are populated and status is retired. |
| ST-036 | Lifecycle | High | Retiring a missing ruleset version fails safely. Details: Call service.retire for a ruleset_id/version that does not exist. | Repository raises a clear not-found error and no rows are changed. |
| ST-037 | Lifecycle | High | Retiring an already retired version does not silently succeed. Details: Retire a version, then attempt to retire it again. | The first retirement succeeds. A second retirement raises RepositoryError and does not overwrite the original retirement metadata. |
| ST-038 | Runtime Spark | Critical | Spark runtime evaluates simple row-level rules correctly. Details: Evaluate representative Spark rows with a published ruleset through service.evaluate_dataframe. | Matched flags, matched rule IDs, assignments, winning-rule trace, and errors match expected outcomes. |
| ST-039 | Runtime Spark | High | Spark runtime honors rule_order and stop_on_match. Details: Evaluate rows where multiple rules can match and the first rule has stop_on_match configured. | Assignments and matched rule IDs reflect ordered evaluation and stop behavior. |
| ST-040 | Runtime Spark | High | Spark row evaluator applies null_input_mode and null_result_mode correctly. Details: Evaluate rows containing null fields across propagate, zero, input-error, result-null, result-default, and result-error modes where applicable. | Results match documented null semantics and errors are emitted only where expected. |
| ST-041 | Runtime Spark | High | Spark runtime handles precomputed aggregate facts as fields. Details: Evaluate a ruleset with upstream aggregate columns over representative records. | Comparisons and assignments match manually calculated expected values. |
| ST-042 | Runtime Spark | Critical | Spark runtime evaluates a published ruleset against a DataFrame. Details: Load a published ruleset by name/version and call service.evaluate_dataframe on a Spark DataFrame. | Output DataFrame contains matched, matched_rule_ids, assign, winning_rule, and error columns with expected values. |
| ST-043 | Runtime Spark | High | Spark runtime fail_on_error behavior is enforced. Details: Evaluate a ruleset/input combination that triggers a runtime error with fail_on_error=True and False. | fail_on_error=True fails the job. fail_on_error=False records errors in output rows. |
| ST-044 | Runtime Spark | High | Spark runtime supports standard custom functions used in rules. Details: Publish and evaluate a ruleset that uses standard functions such as upper, trim, regex_extract, or to_number. | Evaluation succeeds and outputs match expected transformed values. |
| ST-045 | Runtime Spark | Medium | Spark runtime emits native assignment and winning-rule structs. Details: Evaluate a supported ruleset and inspect rules_engine_assign and rules_engine_winning_rule output. | Matched flags, assignments, and winning-rule trace are emitted as Spark-native structs. |
| ST-046 | Auditability | Critical | Payload JSON excludes mutable lifecycle fields and reconstructs ruleset content. Details: Inspect payload_json for a published row and deserialize/load the row. | payload_json contains canonical ruleset content but not lifecycle status. Loading reconstructs the expected ruleset. |
| ST-047 | Auditability | High | Content hash is deterministic and reproducible. Details: Publish or serialize the same ruleset content twice in a controlled test and recompute SHA-256 from payload_json. | content_hash is stable and equals SHA-256 of payload_json bytes. |
| ST-048 | Auditability | Medium | Summary count columns match the published ruleset content. Details: Compare rule_count, condition_count, assignment_count, and custom_function_count to the compiled ruleset. | Persisted counts match the model counts. |
| ST-049 | Pipeline notebooks | Critical | Publish notebook re-instantiates service against existing setup tables. Details: Run notebook 2 after notebook 1 using the same schema or custom table parameters. | Notebook 2 compiles, validates, publishes, and verifies the ruleset without recreating setup objects. |
| ST-050 | Pipeline notebooks | High | Notebook verification checks are present after setup and publish. Details: Review notebooks for assertions that tables exist, standard functions are registered, published row count equals one for target name/version, and load_published succeeds. | Both notebooks fail fast when expected deployment or publish verification conditions are not met. |
| ST-051 | Runtime Spark | Critical | Spark runtime emits condition-level traceability values. Details: Publishes a ruleset that combines a precomputed aggregate field, a standard upper() function, and a row-level field condition; evaluates matched and unmatched rows; verifies winning_rule includes source columns, evaluated operand values, comparison results, pass/fail state, assignments, and the author-facing winning-rule explanation. | winning_rule exposes source columns, evaluated operand values, comparison results, and readable author-facing winning-rule output. |
| ST-052 | Auditability | High | Service describes published rules in human-readable form. Details: Publishes a ruleset with nested boolean logic, precomputed aggregate fields, and a custom-function condition; calls service.describe_rules by ruleset name and version; verifies each returned audit row has readable rule logic and match payload text. | describe_rules returns one readable row per rule with rule logic and match payload details. |
| ST-053 | Runtime Spark | Critical | Spark runtime preserves expected output columns. Details: Publishes a simple ruleset and evaluates matched and unmatched rows; verifies original input columns plus rules_engine_matched, rules_engine_matched_rule_ids, rules_engine_assign, rules_engine_winning_rule, and rules_engine_error columns are present and retain their documented semantics. | Spark runtime input columns and native output columns remain present and usable. |
| ST-054 | Runtime Spark | Critical | Spark runtime emits mapping literal assignments as nested structs. Details: Publishes a ruleset that assigns a literal struct such as non_modeled.market_value/book_value; evaluates a matching row; verifies rules_engine_assign contains a nested Spark struct with selectable boolean child fields. | Mapping literal assignments are emitted as nested Spark structs without stringifying the struct payload. |
| ST-055 | Runtime Spark | Critical | Spark runtime winning-rule explanations use service-style boolean logic. Details: Publishes a ruleset with nested all/any condition groups; evaluates a row where multiple nested branches pass; verifies rules_engine_winning_rule_explanation matches RulesEngineService.describe_rules syntax while winning_rule retains evaluated operand values. | Winning-rule explanations use author-facing service syntax with AND/OR and parentheses, while trace structs retain evaluated values. |
| ST-056 | Runtime Spark | Critical | Spark runtime ignores inactive rule assignment schemas. Details: Publishes a ruleset where an active rule assigns a nested struct and an inactive rule assigns an incompatible value to the same target plus an inactive-only target. | Inactive rules do not alter active assignment struct fields or types. |
| ST-057 | Runtime Spark | High | Spark runtime merges continued multi-match assignments. Details: Publish a ruleset with two matching rules where both use stop_on_match=false and assign the same compatible target; separately validate an incompatible variant. | Compatible assignments keep all matched IDs and use last-writer-wins; incompatible same-target types fail Spark compatibility validation without a string fallback. |
| ST-058 | Runtime Spark | High | Spark runtime serializes only required source fields. Details: Compiles a ruleset with a dotted condition field, a field-backed assignment, an inactive condition field, and an unrelated wide input field; verifies required_source_columns reports only active dependencies and runtime output preserves both evaluation and unrelated source data. | Required source dependencies are deterministic, dotted fields evaluate correctly, inactive and unrelated values are not serialized into the UDF, and original input columns remain present. |
| ST-059 | Runtime Spark | High | Match-only evaluation preserves winning trace and row errors. Details: Evaluates a ruleset where a losing ALL rule has a false first condition and a later null-result error condition, followed by a winning rule; verifies a valid row receives the complete winning-rule trace while the null row still records the later losing-rule error. | Losing rules avoid output trace construction without short-circuiting observable errors, and the first matching rule retains its complete trace and assignment. |
| ST-060 | Runtime Spark | Critical | Financial types cross the real worker boundary exactly. Details: Evaluate Decimal assignments and `to_number` outputs alongside Date, Timestamp, array, and struct assignments on each supported Databricks Runtime. | Values and declared Spark types remain exact; no Decimal-to-double or timestamp-representation coercion occurs. |
| ST-061 | Runtime Spark | High | Fail-fast evaluation is lazy and uses one Python UDF. Details: Build output with `fail_on_error=True`, verify one UDF is constructed without starting a job, then exercise clean and failing data. | Clean output remains lazy until the caller action; a bad row fails that action from the worker without a separate validation scan. |
| ST-062 | Runtime Spark | High | Custom implementations are worker-serializable. Details: Register one approved top-level callable and one implementation capturing an unsupported object. | The approved callable evaluates; the unsupported closure fails serialization preflight before job submission with a focused validation error. |
| ST-063 | Standard functions | Critical | Standard date functions support loan-tape calendar semantics. Details: Evaluate month-end addition, leap-year handling, date differences, typed date/integer assignments, and authored assignment expressions through a real Spark worker. | Month-end and leap-year calculations remain calendar-safe and produce typed assignments with self-contained authored expressions. |
| ST-064 | Publishing | Critical | Embedded expected cases are a hard publication gate. Details: Publish a ruleset whose named cases pass, then attempt publication with a deliberately incorrect expected assignment. | Passing cases publish; failing cases raise before any Delta repository row is written. |
| ST-065 | Auditability | Critical | Audit levels retain immutable execution identity. Details: Evaluate the same row with minimal, standard, and full audit levels and inspect their schemas and identity values. | Each level emits only its documented detail while retaining ruleset ID, version, content hash, and engine version. |
| ST-066 | Change control | High | Semantic diffs compare immutable published versions. Details: Publish two versions with a threshold and assignment change, then call `diff_versions`. | The rendered diff identifies both authored changes and carries distinct baseline/candidate content hashes. |
| ST-067 | Change control | High | Coverage reports dead, broad, and closest rules. Details: Analyze representative loan rows containing first matches and a clean no-match row, then materialize its closest-rule diagnostic. | Counts, dead/broad rule IDs, closest rule, and failed condition IDs match the hand-verified fixture. |

## UAT Tests

| TestID | Area | Priority | Description | ExpectedResult |
| --- | --- | --- | --- | --- |
| UAT-001 | Business authoring | Critical | Business owner can identify the ruleset from YAML metadata. Details: Open the ruleset YAML or notebook summary and review ruleset name, version, owner, department, and description. | Metadata matches the intended business rule set and release candidate. |
| UAT-002 | Business authoring | Critical | Rules listed in YAML match approved business scenarios. Details: Review the rule names, descriptions, order, and plain-English conditions for the release candidate. | Rules correspond to approved business logic and no expected rule is missing. |
| UAT-003 | Business authoring | High | Assignments produced by matching rules are business-meaningful. Details: Review target fields and assigned values for each rule. | Assignment fields and values match business expectations and downstream consumers understand them. |
| UAT-004 | Business authoring | High | Rule order and stop-on-match behavior match business priority. Details: Review scenarios where multiple rules could match the same record. | The chosen rule order and stop behavior produce the intended winning assignment. |
| UAT-005 | Validation messaging | High | Validation errors are understandable to a non-engineering rule author. Details: Run validation against an intentionally invalid ruleset and review the output. | The message identifies what is wrong and where the author should fix the YAML. |
| UAT-006 | Publish readiness | Critical | Validated release candidate can be approved for publishing. Details: Run compile, validation, and every embedded expected case on the candidate ruleset. | No blocking errors are present, every expected case passes, and any warnings are reviewed and accepted before publish. |
| UAT-007 | Publish readiness | Critical | Published version is visible and identifiable in metadata. Details: After publish, review the ruleset_versions row for the candidate. | Business owner can see the expected ruleset_name, version, status, owner, published_by, and published_at. |
| UAT-008 | Version testing | High | Multiple published versions can coexist for comparison testing. Details: Publish current and candidate versions for the same ruleset_name in UAT and review `diff_versions` output. | Both versions are visible, testers can choose the intended version explicitly, and the semantic diff matches the approved change request. |
| UAT-009 | Version testing | High | Testers understand that version is required when multiple published versions exist. Details: Attempt to load by name only when multiple versions are published, then load by explicit version. | Name-only load reports ambiguity when multiple versions exist. Explicit version load succeeds. |
| UAT-010 | Runtime results | Critical | Representative business records receive expected rule outcomes. Details: Run the published ruleset against a curated UAT input dataset and review coverage, first-match distribution, and clean no-match diagnostics. | Each sampled record has the expected outcome, while dead/broad rules and closest-rule diagnostics are understood and accepted. |
| UAT-011 | Runtime results | Critical | Non-matching records remain unassigned or assigned as expected. Details: Include records that should not match any rule or should follow default behavior. | Outputs match the intended non-match behavior without unexpected assignments. |
| UAT-012 | Runtime results | High | Boundary values produce expected outcomes. Details: Test thresholds, equality edges, nulls, blanks, and representative text/case variations. | Boundary records follow approved business semantics. |
| UAT-013 | Runtime results | High | Precomputed aggregate business rules match manual calculations. Details: For rules using upstream aggregate columns, compare engine outputs to manually reviewed aggregate calculations. | Engine results agree with manual calculations for sampled groups or datasets. |
| UAT-014 | Custom functions | Medium | Standard text, number, and date transformations produce recognizable business outcomes. Details: Review trim, case, regex, numeric conversion, month-end/leap-year date addition, and date-difference rules on representative records. | Function-driven outcomes and calendar semantics match what business users expect from the source data. |
| UAT-015 | Operational workflow | High | Setup notebook can be rerun safely before publishing. Details: Rerun setup notebook after tables and function metadata already exist. | Notebook succeeds, refreshes package-owned standard metadata to the installed versions, and creates no duplicate rows. |
| UAT-016 | Operational workflow | High | Publish notebook can be rerun safely for same candidate and handles duplicate publish clearly. Details: Rerun publish notebook for an already-published ruleset_name/version. | Notebook fails or stops with a clear duplicate version message and does not overwrite the original published row. |
| UAT-017 | Operational workflow | High | Retirement workflow removes a version from runtime eligibility. Details: Retire a test version that should no longer be used. | The version status changes to retired and cannot be loaded through published-only load calls. |
| UAT-018 | Operational workflow | Medium | Release evidence is sufficient for audit review. Details: Review publish metadata, content hash, payload_json, expected-case results, semantic diff, coverage output, and notebook run links. | Release evidence is complete enough to support approval and later audit. |
| UAT-019 | Downstream readiness | High | Output DataFrame schema is usable by downstream jobs. Details: Review minimal/standard/full schemas and immutable identity columns with a downstream consumer or integration owner. | The chosen audit level, native fields, match/error outputs, and ruleset/engine identity columns are understood and usable. |
| UAT-020 | Downstream readiness | High | Assignment struct can be consumed by downstream Spark consumers. Details: Consume the assign struct using downstream expected field access. | Assignments can be consumed without manual JSON parsing. |
| UAT-021 | Exception handling | Medium | Runtime error handling is acceptable for business operations. Details: Review behavior when fail_on_error is true and false using a controlled bad input scenario. | The team agrees which mode is appropriate for production and how errors will be monitored. |
| UAT-022 | Documentation | Medium | Notebook instructions are clear enough for a new operator. Details: Ask a tester who did not write the notebooks to run setup and publish in UAT. | Tester can complete the process using notebook instructions and parameters without engineering intervention. |
| UAT-023 | Sign-off | Critical | Business owner approves the published candidate version. Details: Review validation output, UAT outputs, and open defects. | Business owner records approve/defer/reject decision with comments. |
| UAT-024 | Sign-off | Critical | Release manager confirms deployment readiness. Details: Review system test status, UAT status, defect disposition, and rollback/retirement plan. | Release manager signs off or records conditions for release. |
