# Rules Engine System and UAT Test Summary

Source workbook: `outputs/rules_engine_system_test_uat_plan.xlsx`

This Markdown summary was generated by cross-referencing the dated workbook against the current notebook source files:

- `notebooks/rules_engine_system_tests.py`
- `notebooks/rules_engine_uat_tests.py`

The current code is treated as the source of truth for `TestID`, `Area`, `Priority`, and `ExpectedResult`. Workbook execution-step text is included in `Description` where it maps to the current test. The current source contains 62 system tests and 19 UAT tests. Before ST-001, the system notebook locates its repository fixtures and refuses schemas whose names do not visibly identify a disposable test/scratch/tmp target.

## System Tests

| TestID | Area | Priority | Description | ExpectedResult |
| --- | --- | --- | --- | --- |
| ST-001 | Metadata storage | Critical | Test harness creates the target schema before table creation. Details: Run the system notebook against a clean disposable catalog/schema. Use CREATE SCHEMA IF NOT EXISTS before service.create_tables. | The schema exists and the notebook proceeds to table creation without manual pre-work. |
| ST-002 | Metadata storage | Critical | Rules engine metadata tables are created with standard names. Details: Instantiate RulesEngineService.from_schema with the target schema and call service.create_tables(mode='ignore'). | ruleset_versions and function_registry exist in the target schema. Rerunning the cell does not fail. |
| ST-003 | Metadata storage | High | Custom metadata table names are honored by the service. Details: Instantiate RulesEngineService.from_schema with ruleset_versions_table and function_registry_table overrides, then create tables. | The custom tables are created and service.table_names reports the custom names for later publish/load calls. |
| ST-004 | Metadata storage | Critical | Metadata table DDL preserves NOT NULL columns. Details: After table creation, inspect both metadata tables. | Required columns such as ruleset_id, ruleset_name, version, status, effective_start_date, effective_end_date, payload_json, content_hash, rule_count, function_name, implementation_reference, and active_flag are marked NOT NULL where expected. |
| ST-005 | Metadata storage | High | Overwrite mode is restricted to disposable environments. Details: Run with mode='overwrite' only after explicitly approving a disposable schema. | Overwrite recreates tables only when a disposable schema is explicitly approved. |
| ST-006 | Function registry | Critical | Standard function metadata is registered after setup. Details: Call service.save_standard_function_registry after table creation. | function_registry contains all package standard functions with expected implementation_reference, active_flag, allowed flags, return type, and version. |
| ST-007 | Function registry | High | Standard function registration is rerunnable and refreshes package-owned metadata. Details: Run service.save_standard_function_registry twice with its default update behavior. | Both runs succeed, row count remains stable, no duplicates appear, and persisted package versions match the installed standard-function catalog. |
| ST-008 | Function registry | Medium | Explicit preservation mode leaves existing registry rows unchanged. Details: Run service.save_standard_function_registry(update_existing=False) in a controlled test schema. | Existing rows are not overwritten and no duplicate function names are inserted. |
| ST-009 | Function registry | Critical | Runtime in-memory registry contains executable standard implementations. Details: Instantiate RulesEngineService.from_schema with default register_standard=True and inspect registry specs or run a ruleset using standard custom functions. | Standard functions are available for validation and runtime evaluation even though function_registry only stores metadata. |
| ST-010 | Function registry | High | Custom function metadata can be saved without breaking standard registry rows. Details: Save a test custom FunctionRegistryRow through service.save_function_registry_rows. | The custom function row is inserted or updated and existing standard rows remain present. |
| ST-011 | YAML compilation | Critical | Valid canonical ruleset YAML compiles through the service. Details: Call service.compile_yaml_path on a known good ruleset YAML artifact. | A Ruleset object is returned with expected ruleset_id, ruleset_name, version, owner, owner_department, rules, conditions, and assignments. |
| ST-012 | YAML compilation | High | YAML text compilation works for notebook-authored payloads. Details: Call service.compile_yaml_text on a canonical YAML string. | The text compiles into the same model shape as file-based compilation. |
| ST-013 | YAML compilation | Critical | Required top-level ruleset metadata is enforced. Details: Compile YAML missing ruleset_id, ruleset_name, version, or rules list. | Compilation fails before validation or persistence. Error identifies the missing or malformed field. |
| ST-014 | YAML compilation | High | Condition group logical operator shape is enforced. Details: Compile YAML with missing or multiple logical operator keys in a condition group. | Compilation fails and identifies that exactly one logical operator is required. |
| ST-015 | Semantic validation | Critical | Valid simple row-level rule passes validation. Details: Compile a simple ruleset using field/literal comparison and assignment, then run service.validator.validate. | Validation passes with no error-severity issues. |
| ST-016 | Semantic validation | Critical | Owner and owner_department are required before publish. Details: Validate a ruleset missing owner or owner_department. | Validation returns error-severity issues and publish is blocked. |
| ST-017 | Semantic validation | High | Duplicate condition_id values are rejected. Details: Validate a ruleset with duplicate condition IDs across rules or nested groups. | Validation returns a duplicate condition ID error. |
| ST-018 | Semantic validation | High | Duplicate condition_group_id values are rejected. Details: Validate a ruleset with duplicate group IDs. | Validation returns a duplicate condition group ID error. |
| ST-019 | Semantic validation | High | Custom-function argument contracts are enforced. Details: Validate a rule with missing, extra, or misspelled function arguments. | Validation reports the registered contract mismatch. |
| ST-020 | Semantic validation | High | Operator arity and literal collection requirements are enforced. Details: Validate operators such as is_null, between, in, and not_in with invalid right operands. | Validation identifies missing, extra, or malformed operands. |
| ST-021 | Semantic validation | Medium | Operand null defaults must be concrete values. Details: Compile an operand with `default_if_null: null`. | Compilation rejects the null fallback and explains that the option should be omitted when no substitution is wanted. |
| ST-022 | Spark compatibility | Critical | Spark validator accepts supported rulesets. Details: Run SparkRulesetCompatibilityValidator through service.validator on representative supported rulesets. | Validation passes and no Spark-specific unsupported-operation issues are present. |
| ST-023 | Spark compatibility | High | Spark validator follows the supported row-level contract. Details: Validate rulesets with fields, literals, and registered custom functions. | Validation passes for supported row-level rules and rejects base semantic errors. |
| ST-024 | Publishing | Critical | Publish YAML path compiles, normalizes, validates, and writes metadata. Details: Call service.publish_yaml_path on a valid YAML artifact. | One published ruleset_versions row is written with status published, content_hash, payload_json, counts, owner, published_by, and published_at. |
| ST-025 | Publishing | Critical | Direct publish of a compiled ruleset works. Details: Compile YAML, validate it, then call service.publish(ruleset, published_by=...). | The compiled ruleset is persisted and loadable by name/version. |
| ST-026 | Publishing | Critical | Publish rejects non-published lifecycle status. Details: Attempt to publish a compiled ruleset with status retired. | Publish fails before persistence with a clear status requirement message. |
| ST-027 | Publishing | Critical | Duplicate ruleset_name and version cannot be published twice. Details: Publish a ruleset, then attempt to publish another ruleset with the same ruleset_name and version. | Second publish fails and does not overwrite the original row. |
| ST-028 | Publishing | High | Multiple published versions for the same ruleset_name are allowed. Details: Publish version 1 and version 2 for the same ruleset_name. | Both rows have status published. Neither publish requires retiring the other version. |
| ST-029 | Publishing | High | Loading by name without version is rejected when ambiguous. Details: With two published versions for the same ruleset_name, call service.load_published(ruleset_name) without version. | Repository raises an error asking the caller to specify version. |
| ST-030 | Publishing | Critical | Loading by name and version requires one immutable metadata row. Details: Load and evaluate a uniquely published version, append a duplicate of that exact metadata row, then load the same explicit version again. | The unique row reconstructs and evaluates correctly; the duplicate immutable version fails loudly instead of selecting an arbitrary row. |
| ST-031 | Lifecycle | Critical | Retirement makes a version unavailable to load_published. Details: Publish a ruleset, call service.retire(ruleset_id, version), then attempt load_published by name/version. | The row status is retired and load_published no longer returns it. |
| ST-032 | Lifecycle | High | Retirement stamps retired_by and retired_at. Details: Retire a published version with retired_by populated. | retired_by and retired_at are populated and status is retired. |
| ST-033 | Lifecycle | High | Retiring a missing ruleset version fails safely. Details: Call service.retire for a ruleset_id/version that does not exist. | Repository raises a clear not-found error and no rows are changed. |
| ST-034 | Lifecycle | High | Retiring an already retired version does not silently succeed. Details: Retire a version, then attempt to retire it again. | The first retirement succeeds. A second retirement raises RepositoryError and does not overwrite the original retirement metadata. |
| ST-035 | Runtime Spark | Critical | Spark runtime evaluates simple row-level rules correctly. Details: Evaluate representative Spark rows with a published ruleset through service.evaluate_dataframe with full audit enabled. | Matched flags, matched rule IDs, assignments, detailed matched-rule traces, and errors match expected outcomes. |
| ST-036 | Runtime Spark | High | Spark runtime honors rule_order and stop_on_match. Details: Evaluate rows where multiple rules can match and the first rule has stop_on_match configured. | Assignments and matched rule IDs reflect ordered evaluation and stop behavior. |
| ST-037 | Runtime Spark | High | Spark row evaluator applies operand-level null defaults and condition errors correctly. Details: Evaluate null numeric and string fields with no fallback, `default_if_null`, and `error_on_null: true`. | Unresolved nulls do not match by default, typed fallbacks are compared, full audit shows substitution, and errors occur only when explicitly requested. |
| ST-038 | Runtime Spark | Critical | Spark runtime evaluates a published ruleset against a DataFrame. Details: Load a published ruleset by name/version and call service.evaluate_dataframe on a Spark DataFrame. | Output DataFrame contains error, matched, matched_rule_ids, assign, ruleset identity, and engine version columns with expected values. |
| ST-039 | Runtime Spark | High | Spark runtime fail_on_error behavior is enforced. Details: Evaluate a ruleset/input combination that triggers a runtime error with fail_on_error=True and False. | fail_on_error=True fails the job. fail_on_error=False records errors in output rows. |
| ST-040 | Runtime Spark | High | Spark runtime supports standard custom functions used in rules. Details: Publish and evaluate a ruleset that uses standard functions such as upper, trim, regex_extract, or to_number. | Evaluation succeeds and outputs match expected transformed values. |
| ST-041 | Runtime Spark | Medium | Spark runtime emits native assignment and trace structs. Details: Evaluate a supported ruleset with full audit and inspect `rules_engine_assign` and `rules_engine_matched_rules`. | Matched flags, assignments, and each detailed matched-rule trace are emitted as Spark-native structs. |
| ST-042 | Auditability | Critical | Payload JSON excludes mutable lifecycle fields and reconstructs ruleset content. Details: Inspect payload_json for a published row and deserialize/load the row. | payload_json contains canonical ruleset content but not lifecycle status. Loading reconstructs the expected ruleset. |
| ST-043 | Auditability | High | Content hash is deterministic and reproducible. Details: Publish or serialize the same ruleset content twice in a controlled test and recompute SHA-256 from payload_json. | content_hash is stable and equals SHA-256 of payload_json bytes. |
| ST-044 | Auditability | Medium | Summary count columns match the published ruleset content. Details: Compare rule_count, condition_count, assignment_count, and custom_function_count to the compiled ruleset. | Persisted counts match the model counts. |
| ST-045 | Runtime Spark | Critical | Spark runtime emits condition-level traceability values. Details: Publish a ruleset that combines a numeric field, a standard upper() function, and a string field condition; evaluate matched and unmatched rows with full audit; verify each `matched_rules` trace includes source columns, original and effective operand values, comparison results, pass/fail state, assignments, and an author-facing explanation. | `matched_rules` exposes complete row-specific traces for every match. |
| ST-046 | Auditability | High | Service describes published rules in human-readable form. Details: Publish a ruleset with nested boolean logic and a custom-function condition; call service.describe_rules by ruleset name and version; verify each returned audit row has readable rule logic and match payload text. | describe_rules returns one readable row per rule with rule logic and match payload details. |
| ST-047 | Runtime Spark | Critical | Spark runtime preserves expected output columns. Details: Publishes a simple ruleset and evaluates matched and unmatched rows; verifies original input columns plus the documented compact runtime columns are present and retain their semantics. | Spark runtime input columns and native output columns remain present and usable. |
| ST-048 | Runtime Spark | Critical | Spark runtime emits mapping literal assignments as nested structs. Details: Publishes a ruleset that assigns a literal struct such as non_modeled.market_value/book_value; evaluates a matching row; verifies rules_engine_assign contains a nested Spark struct with selectable boolean child fields. | Mapping literal assignments are emitted as nested Spark structs without stringifying the struct payload. |
| ST-049 | Runtime Spark | Critical | Spark runtime match-trace explanations use service-style boolean logic. Details: Publishes a ruleset with nested all/any condition groups; evaluates a row with full audit where multiple nested branches pass; verifies the nested trace explanation matches RulesEngineService.describe_rules syntax while the trace retains evaluated operand values. | Match-trace explanations use author-facing service syntax with AND/OR and parentheses, while trace structs retain evaluated values. |
| ST-050 | Runtime Spark | Critical | Spark runtime ignores inactive rule assignment schemas. Details: Publishes a ruleset where an active rule assigns a nested struct and an inactive rule assigns an incompatible value to the same target plus an inactive-only target. | Inactive rules do not alter active assignment struct fields or types. |
| ST-051 | Runtime Spark | High | Spark runtime merges continued multi-match assignments. Details: Publish a ruleset with two matching rules where both use stop_on_match=false and assign the same compatible target; separately validate an incompatible variant. | Compatible assignments keep all matched IDs and use last-writer-wins; incompatible same-target types fail Spark compatibility validation without a string fallback. |
| ST-052 | Runtime Spark | High | Spark runtime serializes only required source fields. Details: Compiles a ruleset with a dotted condition field, a field-backed assignment, an inactive condition field, and an unrelated wide input field; verifies required_source_columns reports only active dependencies and runtime output preserves both evaluation and unrelated source data. | Required source dependencies are deterministic, dotted fields evaluate correctly, inactive and unrelated values are not serialized into the UDF, and original input columns remain present. |
| ST-053 | Runtime Spark | High | Full-audit evaluation preserves matched-rule traces and row errors without duplicate evaluation. Details: Evaluates a ruleset where a losing ALL rule has a false first condition and a later `error_on_null` condition, followed by a matching rule; verifies a valid row receives only the matched rule's complete trace while the null row records the later losing-rule error. | Every reached active condition is evaluated once, losing rules emit no trace, and every matching rule retains its complete trace and assignment. |
| ST-054 | Runtime Spark | Critical | Financial types cross the real worker boundary exactly. Details: Evaluate Decimal assignments and `to_number` outputs alongside Date, Timestamp, array, and struct assignments on each supported Databricks Runtime. | Values and declared Spark types remain exact; no Decimal-to-double or timestamp-representation coercion occurs. |
| ST-055 | Runtime Spark | High | Fail-fast evaluation is lazy and uses one Python UDF. Details: Build output with `fail_on_error=True`, verify one UDF is constructed without starting a job, then exercise clean and failing data. | Clean output remains lazy until the caller action; a bad row fails that action from the worker without a separate validation scan. |
| ST-056 | Runtime Spark | High | Custom implementations are worker-serializable. Details: Register one approved top-level callable and one implementation capturing an unsupported object. | The approved callable evaluates; the unsupported closure fails serialization preflight before job submission with a focused validation error. |
| ST-057 | Standard functions | Critical | Standard date functions support loan-tape calendar semantics. Details: Evaluate month-end addition, leap-year handling, date differences, typed date/integer assignments, and authored assignment expressions through a real Spark worker. | Month-end and leap-year calculations remain calendar-safe and produce typed assignments with self-contained authored expressions. |
| ST-058 | Publishing | Critical | Embedded expected cases are a hard publication gate. Details: Publish a ruleset whose named cases pass, then attempt publication with a deliberately incorrect expected assignment. | Passing cases publish; failing cases raise before any Delta repository row is written. |
| ST-059 | Auditability | Critical | Compact and full-audit outputs retain immutable execution identity. Details: Evaluate the same row with `full_audit=False` and `True`, then inspect schema ordering and identity values without coupling the test to packaged-wheel metadata. | Each mode emits only its documented detail while retaining the ruleset struct and a non-empty engine version. |
| ST-060 | Change control | High | Semantic diffs compare immutable published versions. Details: Publish two versions with a threshold and assignment change, then call `diff_versions`. | The rendered diff identifies both authored changes and carries distinct baseline/candidate content hashes. |
| ST-061 | Change control | High | Coverage reports dead, broad, and closest rules. Details: Analyze representative loan rows containing first matches and a clean no-match row, then materialize its closest-rule diagnostic. | Counts, dead/broad rule IDs, closest rule, and failed condition IDs match the hand-verified fixture. |
| ST-062 | Runtime Spark | Critical | Later rules consume explicit prior assignment results. Details: Evaluate a producer/consumer chain, a missing-producer row with a fallback, original-row and assigned reads of the same name, and two sibling assignments that would differ under sequential mutation. | Later rules see lower-order commits, field operands retain original-row semantics, missing commits follow null defaults, sibling assignments read one pre-rule snapshot, and full audit identifies the producing rule and assignment. |

## UAT Tests

| TestID | Area | Priority | Description | ExpectedResult |
| --- | --- | --- | --- | --- |
| UAT-001 | Business authoring | Critical | Business owner can identify the ruleset from YAML metadata. Details: Open the ruleset YAML or notebook summary and review ruleset name, version, owner, department, and description. | Metadata matches the intended business rule set and release candidate. |
| UAT-002 | Business authoring | Critical | Rules listed in YAML match approved business scenarios. Details: Review the rule names, descriptions, order, and plain-English conditions for the release candidate. | Rules correspond to approved business logic and no expected rule is missing. |
| UAT-003 | Business authoring | High | Assignments produced by matching rules are business-meaningful. Details: Review target fields and assigned values for each rule. | Assignment fields and values match business expectations and downstream consumers understand them. |
| UAT-004 | Business authoring | High | Rule order and stop-on-match behavior match business priority. Details: Review scenarios where multiple rules could match the same record. | The chosen rule order and stop behavior produce the intended combined assignments. |
| UAT-005 | Validation messaging | High | Validation errors are understandable to a non-engineering rule author. Details: Run validation against an intentionally invalid ruleset and review the output. | The message identifies what is wrong and where the author should fix the YAML. |
| UAT-006 | Publish readiness | Critical | Validated release candidate can be approved for publishing. Details: Run compile, validation, and every embedded expected case on the candidate ruleset. | No blocking errors are present, every expected case passes, and any warnings are reviewed and accepted before publish. |
| UAT-007 | Publish readiness | Critical | Published version is visible and identifiable in metadata. Details: After publish, review the ruleset_versions row for the candidate. | Business owner can see the expected ruleset_name, version, status, owner, published_by, and published_at. |
| UAT-008 | Version testing | High | Multiple published versions can coexist for comparison testing. Details: Publish current and candidate versions for the same ruleset_name in UAT and review `diff_versions` output. | Both versions are visible, testers can choose the intended version explicitly, and the semantic diff matches the approved change request. |
| UAT-009 | Version testing | High | Testers understand that version is required when multiple published versions exist. Details: Attempt to load by name only when multiple versions are published, then load by explicit version. | Name-only load reports ambiguity when multiple versions exist. Explicit version load succeeds. |
| UAT-010 | Runtime results | Critical | Representative business records receive expected rule outcomes. Details: Run the published ruleset against a curated UAT input dataset and review coverage, first-match distribution, and clean no-match diagnostics. | Each sampled record has the expected outcome, while dead/broad rules and closest-rule diagnostics are understood and accepted. |
| UAT-011 | Runtime results | Critical | Non-matching records remain unassigned or assigned as expected. Details: Include records that should not match any rule or should follow default behavior. | Outputs match the intended non-match behavior without unexpected assignments. |
| UAT-012 | Runtime results | High | Boundary values produce expected outcomes. Details: Test thresholds, equality edges, nulls, blanks, and representative text/case variations. | Boundary records follow approved business semantics. |
| UAT-013 | Custom functions | Medium | Standard text, number, and date transformations produce recognizable business outcomes. Details: Review trim, case, regex, numeric conversion, month-end/leap-year date addition, and date-difference rules on representative records. | Function-driven outcomes and calendar semantics match what business users expect from the source data. |
| UAT-014 | Operational workflow | High | Retirement workflow removes a version from runtime eligibility. Details: Retire a test version that should no longer be used. | The version status changes to retired and cannot be loaded through published-only load calls. |
| UAT-015 | Operational workflow | Medium | Release evidence is sufficient for audit review. Details: Review publish metadata, content hash, payload_json, expected-case results, semantic diff, coverage output, and notebook run links. | Release evidence is complete enough to support approval and later audit. |
| UAT-016 | Downstream readiness | High | Output DataFrame schema is usable by downstream jobs. Details: Review compact/full-audit schemas and immutable identity columns with a downstream consumer or integration owner. | The chosen audit mode, native fields, match/error outputs, and ruleset/engine identity columns are understood and usable. |
| UAT-017 | Downstream readiness | High | Assignment struct can be consumed by downstream Spark consumers. Details: Consume the assign struct using downstream expected field access. | Assignments can be consumed without manual JSON parsing. |
| UAT-018 | Exception handling | Medium | Runtime error handling is acceptable for business operations. Details: Review behavior when fail_on_error is true and false using a controlled bad input scenario. | The team agrees which mode is appropriate for production and how errors will be monitored. |
| UAT-019 | Sign-off | Critical | Business owner approves the published candidate version. Details: Review validation output, UAT outputs, and open defects. | Business owner records approve/defer/reject decision with comments. |
