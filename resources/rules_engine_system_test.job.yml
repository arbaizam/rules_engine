resources:
  jobs:
    rules_engine_system_test:
      name: ${bundle.target}_rules_engine_system_test
      tasks:
        - task_key: system_test
          existing_cluster_id: ${var.existing_cluster_id}
          spark_python_task:
            python_file: ./databricks/system_test_rules_engine.py
          libraries:
            - whl: ./dist/*.whl
